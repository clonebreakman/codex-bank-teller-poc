# P4 Gate Record

Status: passed for the constrained local runner and browser boundary.

Evidence:

- Run state machine supports created/classified/approved/executing/verifying and terminal completed/handoff/denied/failed states.
- Illegal transitions and post-terminal execution are rejected.
- Offline Mock Model proposes only the registered balance workflow for the first slice.
- Model proposals are rejected when workflow or tool is not registered.
- Browser origins are limited to explicitly configured local HTTP origins.
- Browser action allowlist excludes uploads, downloads, arbitrary commands, and external navigation.
- Observations are single-use; stale observation actions are rejected.
- Sensitive typed text is summarized as `[REDACTED]`.
- Targeted P4 tests: 12 passed (10 package tests plus 2 root security tests).

Computer Use integration remains disabled by default and must stay inside the synthetic local environment.
