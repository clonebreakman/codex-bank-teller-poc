# P6 Gate Record

Status: passed for the local synthetic Stage-A scenario acceptance. This gate does not authorize production banking access, real customer data, real credentials, payment networks, or financial writes.

Scenario matrix:

- 8 green read-only scenarios: balance, transactions, receipts, branch hours, fee schedule, ticket status, missing documents, and customer summary.
- 1 yellow reversible-write boundary: appointment draft. The policy returns `confirm`, and the tool is not executed automatically.
- 1 red security boundary: prompt-injection page. Detection stops the flow and requires a verified security event; page content is never treated as authorization.

Evidence:

- Ten scenario contracts are registered under `scenarios/`, and every scenario is marked `syntheticOnly: true`.
- Every successful scenario result is checked by an external verifier.
- Identity mismatch stops execution with `handoff` and `IDENTITY_MISMATCH`.
- Appointment-draft boundary verification proves `confirm` plus `executed: false`.
- Prompt-injection detection and security-event verification pass, with no tool execution for the malicious page case.
- The local acceptance test performs 160 green attempts across 20 repetitions, with a success rate of at least 99% and 0 policy violations.
- Latest full test run: 23 files and 66 tests passed.
- Latest security test run: 3 files and 7 tests passed.
- `eslint .`: exit code 0.
- `tsc --noEmit`: exit code 0.

Scope and next gate:

- This is a local engineering acceptance record only; business, security, and operational sign-off is still required before any broader pilot.
- P1 remains partial because PostgreSQL schema/seed recreation and Docker Compose rebuild verification have not been run with a live Docker daemon.
- The next implementation gate is P1.2 PostgreSQL reconstruction, followed by the P7 internal teller copilot only after its own review and approval.
